name = "nuhah-serp-mcp"
main = "src/worker.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

# Set these via `wrangler secret put`:
# SERP_API_KEY  (your SerpAPI key from https://serpapi.com/manage-api-key)

[vars]
# Non-secret defaults
DEFAULT_GL = "uk"
DEFAULT_HL = "en"
DEFAULT_TRENDS_GEO = "GB"
